# Program to calculate factorial of a number

num = int(input("Enter number: "))

fact = 0                            # ⚠️ Logical: should start with 1 (otherwise multiplication yields 0)

for i in range(1, num + 1)          # ❌ Missing colon
    fact = fact * i                 # ⚠️ multiplies by 0 due to fact initialized to 0
    print("Current factorial:", fact)

if num < 0                           # ❌ Missing colon
    print("Negative numbers not allowed")
elif num == 0:
    print("Factorial is 1")
else                                  # ❌ Missing colon
    print("Final factorial is", fact)

pritn("Calculation done")           # ❌ Typo: print misspelled
print("Result:" fact)               # ❌ Missing comma between arguments
